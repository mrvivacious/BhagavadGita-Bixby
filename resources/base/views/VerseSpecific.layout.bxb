layout {
  match: VerseByLookupConcept (this)

  // Use "Summary" flag for DEBUG
  mode (Details)

  content {
    section {
      content {
        // paragraph {
        //   value ("#{value(this)}")
        //   style (Title_L)
        // }
      }
    }
  }
}
