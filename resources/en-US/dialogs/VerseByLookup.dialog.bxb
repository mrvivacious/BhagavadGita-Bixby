dialog (Result) {
  match: 
    VerseByLookupConcept (lookupResult) {
      from-output: GetVerseSpecific (lookupOutput)
    }

  // template("You've got a ${value(lookupResult.chapter)}:${value(lookupResult.verse)}")
    template("${value(lookupResult.SlokaSanskrit)}\n\n${value(lookupResult.SlokaMeaning)}")

}
